<!DOCTYPE html>

{{#if user}}
<div class="well">
    <h4>Laisser un commentaire :</h4>
    <form action="/comment/?id={{establishment.id}}" role="form" method="POST" enctype="multipart/form-data">
        <div class="form-group">
            <div class="fallback dropzone">
                <input type="file"name="picture">
            </div>
            <hr>
            <input type=text" class="form-control" name="comment" rows="3" placeholder="Comment">
        </div>
        <div class="row">
            <div class="col-md-6">
                <input type="text" name="rating" class="rating" data-size="xs">
            </div>
            <div class="col-md-6">
                <button type="submit" class="btn btn-primary pull-right">Envoyer</button>
            </div>
        </div>
    </form>
</div>

<hr>
{{/if}}

{{#each comments}}
<div class="well">
    <div class="row">
        {{#if picture_attached}}
        <div class="col-md-3">
            <img class="media-object img-responsive" data-action="zoom" src="/user/{{username}}/comment/{{timestamp}}/picture" alt="">
        </div>

        <div class="col-md-9">
        {{else}}
        <div class="col-md-12">
        {{/if}}
            <div class="row">
                <div class="col-md-6 pull-left">
                    <span class="glyphicon glyphicon-user" aria-hidden="true"><a href="/user/{{username}}">{{username}}</a></span>
                    </br>
                    <span class="glyphicon glyphicon-calendar" aria-hidden="true">{{timestamp}}</span>
                </div>
                <div class="col-md-6 pull-right">
                    {{rating}} étoiles
                    {{#if ../user.admin}}
                    </br>
                    <a href="/comment/remove/{{id}}">
                        <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                    </a>
                    {{/if}}
                </div>
            </div>

            <hr>

            <div class="row">
                {{comment_text}}
            </div>
        </div>
    </div>
</div>
{{/each}}

